---
description: Trivial reachability test for the native slash-command mechanism (Phase 0.2 / G0).
---

Reply with exactly:

**pong** — native `.claude/commands/` is reachable in `prototype-builder v.2`. (Phase 0.2 ✓)

Then stop.
