---
description: Generate or update the Data tab — a field / type / example table plus a Mermaid erDiagram for the prototype's eventual data model (5 guardrails — PK, FK, cardinality, naming, completeness). Decoupled and manual; never auto-fires.
---

# /pb-sync-erd — (Phase 1 skeleton)

**Status:** stub — full behavior lands in **Phase 5** (ported from v0.4.0 `sync-erd`).
**Purpose:** field/type/example table + Mermaid ERD into the Data tab, on demand.

When invoked now, state that this is a Phase-1 stub and print the purpose above. See [router](../../CLAUDE.md).
