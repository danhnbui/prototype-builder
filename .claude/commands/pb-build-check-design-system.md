---
description: Sub-command of /pb-build. Scan the global component index by function/purpose, then reuse, extend with a variant (R2), or build a local component; enforce the naming contract (kebab-case IDs, renderCmp{PascalCase}, token schema, no raw hex/px).
---

# /pb-build-check-design-system — (Phase 1 skeleton)

**Status:** stub — full behavior lands in **Phase 3** (net-new; today it is the inline G2 gate inside v0.4.0 `build.md`).
**Purpose:** DS-first reuse vs extend-variant vs build-local; enforce the naming contract.

When invoked now, state that this is a Phase-1 stub and print the purpose above. See [router](../../CLAUDE.md).
