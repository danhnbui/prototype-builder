---
description: Generate or update the UX Design / Flow tab — a Mermaid wireflow diagram plus a numbered user-story test checklist. Decoupled and manual; never auto-fires.
---

# /pb-sync-flow — (Phase 1 skeleton)

**Status:** stub — full behavior lands in **Phase 5** (ported from v0.4.0 `sync-flow`).
**Purpose:** wireflow (Mermaid flowchart) + test checklist into the Flow tab, on demand.

When invoked now, state that this is a Phase-1 stub and print the purpose above. See [router](../../CLAUDE.md).
