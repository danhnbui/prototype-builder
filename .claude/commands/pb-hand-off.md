---
description: Hand off the prototype in one of two modes. --people produces a view-only, self-documenting prototype.html with an auto-generated cover (authoring CTAs hidden). --context exports a portable bundle (registry.json + design-system.md + constitution.md + decisions.md) that /pb-init --import can ingest.
---

# /pb-hand-off — (Phase 1 skeleton)

**Status:** stub — full behavior lands in **Phase 6**.
**Purpose:** `--people` (view-only self-documenting file + cover) · `--context` (portable registry + memory + DS bundle). Auto-renders the HTML first.

When invoked now, state that this is a Phase-1 stub and print the purpose above. See [router](../../CLAUDE.md).
