---
description: Read-only drift audit of the trio (screens, components, logic) against memory/constitution.md. Produces a report only — never writes to registry.json or prototype.html. Run after long sessions to catch contradictions that slipped past per-prompt checks.
---

# /pb-check-drift — (Phase 1 skeleton)

**Status:** stub — full behavior lands in **Phase 5** (ported from v0.4.0 `check-drift`).
**Purpose:** read-only audit of the trio vs `constitution.md`; report only, no writes.

When invoked now, state that this is a Phase-1 stub and print the purpose above. See [router](../../CLAUDE.md).
