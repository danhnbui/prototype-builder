---
description: Scaffold a new Product Builder prototype — PRD intake (Q&A or file, never blank), set Stack + DS locks, seed registry.json + memory/. Optional --import <bundle> to ingest a context hand-off.
---

# /pb-init — (Phase 1 skeleton)

**Status:** stub — full behavior lands in **Phase 4**.
**Purpose:** scaffold a prototype: PRD intake, set Stack + DS locks, seed `registry.json` + `memory/`; optional `--import`.

When invoked now, state that this is a Phase-1 stub and print the purpose above. See [router](../../CLAUDE.md) for the command map and the three loop rules.
