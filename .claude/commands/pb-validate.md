---
description: Scaffold a real Vite or Next build from the prototype. Auto-renders prototype.html from registry.json first, then generates a buildable, runnable project skeleton.
---

# /pb-validate — (Phase 1 skeleton)

**Status:** stub — full behavior lands in **Phase 6**.
**Purpose:** scaffold a Vite / Next build from `prototype.html` (auto-render first); the scaffold builds and runs locally.

When invoked now, state that this is a Phase-1 stub and print the purpose above. See [router](../../CLAUDE.md).
