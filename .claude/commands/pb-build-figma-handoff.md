---
description: Sub-command of /pb-build. The ported figma-push — one-way registry → Figma transfer via the Figma MCP, with a 5-gate clarify pass (G-FP1..G-FP5), DS-neutral component match (dsMatch.library from config, not hardcoded), and auto-layout on every created frame.
---

# /pb-build-figma-handoff — (Phase 1 skeleton)

**Status:** stub — full behavior lands in **Phase 3** (ported from v0.4.0 `figma-push.md`, DS-neutralized).
**Purpose:** 5-gate, DS-neutral, one-way push to Figma with auto-layout (R3).

When invoked now, state that this is a Phase-1 stub and print the purpose above. See [router](../../CLAUDE.md).
