---
description: "[Deprecated alias] Renamed to /pb:data in v1.5.1. Generates the Data tab."
---

# /pb:sync-erd — alias for `/pb:data`

`/pb:sync-erd` was renamed to **`/pb:data`** in v1.5.1. This alias keeps existing muscle
memory, scripts, and older projects working; it will be removed in a future major release.

**Execute [`data.md`](data.md) exactly**, as if the user had invoked `/pb:data`, preserving
any arguments passed to `/pb:sync-erd` (e.g. `--mock`).
