---
description: "[Deprecated alias] Renamed to /pb:flow in v1.5.1. Generates the UX Design / Flow tab."
---

# /pb:sync-flow — alias for `/pb:flow`

`/pb:sync-flow` was renamed to **`/pb:flow`** in v1.5.1. This alias keeps existing muscle
memory, scripts, and older projects working; it will be removed in a future major release.

**Execute [`flow.md`](flow.md) exactly**, as if the user had invoked `/pb:flow`, preserving
any arguments passed to `/pb:sync-flow`.
